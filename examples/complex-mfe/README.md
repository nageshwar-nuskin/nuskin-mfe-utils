# complex-mfe (legacy standalone)

**Prefer [@nuskin/mfe-examples](../../packages/mfe-examples/)** — source of truth for `complex_demo_mfe`.

```bash
# Default — no separate server:
yarn dev   # from repo root → gateway :3100 serves /mfe-assets/complex_demo_mfe/

# Optional isolated federation debug:
cd examples/complex-mfe && yarn install && yarn build && yarn start   # :5512
```

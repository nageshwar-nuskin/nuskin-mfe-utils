# minimal-mfe (legacy standalone)

**Prefer [@nuskin/mfe-examples](../../packages/mfe-examples/)** — one `yarn dev` at repo root (gateway only).

This folder is an optional **standalone federation sandbox** if you need to debug webpack remotes in isolation:

```bash
yarn install && yarn build && yarn start   # port 5510
```
